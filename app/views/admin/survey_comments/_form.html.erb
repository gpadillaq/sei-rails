<%= form_for([:admin, survey_comment]) do |f| %>
  <% if survey_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(survey_comment.errors.count, "error") %> prohibited this survey_comment from being saved:</h2>

      <ul>
      <% survey_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_survey_id %>
    <%= f.text_field :user_survey_id %>
  </div>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_area :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
